{% extends 'base.html.twig' %}

{% block title %}Trombinoscope DWWM-2{% endblock %}

{% block body %}
    

    <main class="main-content container">
        {% for student in students %}
        <div class="card-container home-card">
            <div class="card">
                <img src="{{ asset('assets/img/' ~ student.imgSrc) }}" alt="{{student.prenom}} {{ student.nom }}">
                <h2><a href="{{ path("student", {'id': student.id}) }}">{{student.prenom}} {{student.nom}}</a></h2>
                <a href="{{ path("student", {'id': student.id}) }}" class="button"><span>En savoir +</span></a>
            </div>
        </div>
        <div class="card-container home-card">
            <div class="card">
                <img src="{{ asset('assets/img/' ~ student.imgSrc) }}" alt="{{student.prenom}} {{ student.nom }}">
                <h2><a href="{{ path("student", {'id': student.id}) }}">{{student.prenom}} {{student.nom}}</a></h2>
                <a href="{{ path("student", {'id': student.id}) }}" class="button"><span>En savoir +</span></a>
            </div>
        </div>
        <div class="card-container home-card">
            <div class="card">
                <img src="{{ asset('assets/img/' ~ student.imgSrc) }}" alt="{{student.prenom}} {{ student.nom }}">
                <h2><a href="{{ path("student", {'id': student.id}) }}">{{student.prenom}} {{student.nom}}</a></h2>
                <a href="{{ path("student", {'id': student.id}) }}" class="button"><span>En savoir +</span></a>
            </div>
        </div>
        <div class="card-container home-card">
            <div class="card">
                <img src="{{ asset('assets/img/' ~ student.imgSrc) }}" alt="{{student.prenom}} {{ student.nom }}">
                <h2><a href="{{ path("student", {'id': student.id}) }}">{{student.prenom}} {{student.nom}}</a></h2>
                <a href="{{ path("student", {'id': student.id}) }}" class="button"><span>En savoir +</span></a>
            </div>
        </div>
        {% endfor %}
    </main>

{% endblock %}
