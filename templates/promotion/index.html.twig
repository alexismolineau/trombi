{% extends 'base.html.twig' %}

{% block title %}Hello PromotionController!{% endblock %}

{% block body %}

<div>
    <h2> Liste des promotions </h2>
    <ul>
        {% for promotion in promotions %}
        <li><a href="{{ path("students_list", {'id': promotion.id}) }}">{{promotion.nom}}</a></li>
        {% endfor %}
    </ul>
</div>
{% endblock %}
